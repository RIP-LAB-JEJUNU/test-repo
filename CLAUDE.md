# CLAUDE.md

이 파일은 **Claude Code가 이 저장소에서 세션을 시작할 때 자동으로 읽는 지침서**입니다.
여기 적어둔 규칙은 별도로 말하지 않아도 Claude가 따릅니다.
저장소 단위로 동작하므로, 프로젝트마다 내용이 달라도 됩니다.

> **현재 이 파일은 빈 템플릿입니다. 실질적인 규칙이 아직 없습니다.**
>
> 프로젝트를 시작할 때 Claude와 상의하면서 채우세요. 예를 들어
> *"저장소 구조를 훑어보고 CLAUDE.md에 들어갈 내용을 제안해줘"*
> 라고 하면 빌드 방법이나 관례를 파악해 초안을 만들어 줍니다.
> 다 채운 뒤에는 이 인용문을 지우세요.

## 프로젝트 개요

(무엇을 하는 코드인지 한두 문장. 도메인 용어가 있으면 같이.)

## 빌드 · 실행

(실제 명령어. Claude가 변경 후 검증할 때 이걸 씁니다.)

```bash
# 예: cmake -S . -B build && cmake --build build
# 예: pytest tests/
```

## 이 저장소의 규칙

- 그림은 코드로 생성한다. 저장소에 커밋하지 않는다.
- 데이터는 저장소 밖에 둔다. 경로는 README에 적는다.
- 안 쓰는 파일과 코드는 남기지 않는다. 지울 수 있으면 지운다.
- 파일명에 `final`, `v2`, `polished` 같은 접미사를 붙이지 않는다.
  버전 관리는 git이 한다.
- 변경한 코드는 실제로 실행해서 확인한 뒤 커밋한다.

## 주의사항

(건드리면 안 되는 부분, 빠지기 쉬운 함정, 물리적으로 틀리기 쉬운 계산 등.
 여기에 적어두면 같은 실수가 반복되지 않습니다.)
